extends layout

block content
    h1 #{title}
    form(method='POST', action='/gates/add')
        #form-group
            label Gate Name:
            input.form-control(name='name', type='text')
        br
        input.btn.btn-primary(type='submit', value='Submit')